// let arr = [];
// let size = prompt("Enter the size of array");
// for (let i = 0; i < size; i++) {
//   arr[i] = Number(prompt("Enter karo value"));
// }
// let max = -Infinity,
//   min = Infinity;
// for (let i = 0; i < arr.length; i++) {
//   if (arr[i] > max) max = arr[i];
//   if (arr[i] < min) min = arr[i];
// }
// alert(`min = ${min} , max = ${max}`);


let arr = [];
let size = prompt("Enter the size of array ")
let sum =0;
for(let i=0;i<arr.size;i++){
  if (!isNaN(value) && !(Math.sign(value)==0)&& !(Math.sign(value)==-1)){
    sum +=value;}}
  // sum +=arr[i]

//     console.log(sum);
// // }
// arr.forEach(value => {
//   if (!isNaN(value) && !(Math.sign(value)>1)&& !(Math.sign(value)==1)){
//     sum +=value;}
// });
alert(sum);


// let arr =[];
// let arr2 = prompt("Enter the value what you want");
// for (const value of arr) {
//   if (!isNaN(value) && !(Math.sign(value)==0)&& !(Math.sign(value)==-1)){
//     sum +=value;
//   }
  
// }
// alert(sum);